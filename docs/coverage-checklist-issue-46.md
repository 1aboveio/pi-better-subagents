## Coverage Checklist

_Every row = a decision (PASS / FAIL / N/A) backed by a checkable evidence locator (`file:line`, a rerunnable `command`, or #issue). Gate rows are auto-filled by `coverage-checklist.mjs generate`; lead/judgment rows are completed by the reviewer. A PASS/FAIL row with no locator, or a gate row left TODO, fails `coverage-checklist.mjs validate`._

Issue: #46 (live detail view navigation in the subagent navigator). Scope class: `surface-bearing` (`scope-class.mjs classify --stdin` over `5c9fb9d...HEAD` → unmatched runtime paths include `index.ts`, `navigator.mjs`, `navigator.ts`). Stacked unit: base is `issue-45-minimal-subagent-navigator` (`5c9fb9d`), not `main`.

### Breadth

| Check | Method | Decision | Evidence |
|---|---|---|---|
| Surface inventory + route manifest regenerated; no drift | gate-lint | PASS | `docs/tests/_generated/surface-inventory.json` regenerated with `navigator.detail` (mutation); no browser route manifest exists in this pi extension (precedent: `docs/coverage-checklist-issue-45.md`) |
| Every changed surface is owned and proven at its floor (no Missing/Orphan) | gate-lint | PASS | command: `node coverage-ledger.mjs validate` (via import of `build`) → "16 surfaces · 16 aligned · 0/16 tested surfaces over-mocked" — zero Missing/Orphan/Unrealized for the 1 #46 surface (owned by `docs/tests/issue-46-detail-subagent-navigator.coverage.yml`, proven by `tests/navigator_detail.test.mjs`); exit 1 is solely `Unverified` attestation rows (15 pre-existing across #13/#39/#44/#45 + 1 #46 row a producer may not self-attest — awaiting reviewer promotion per ensure-coverage) |
| No un-specced capability missed — the surfaces no AC mentions | judgment | PASS | issue #46 ACs cover Enter-to-detail, rendered detail fields, 1s live refresh, running-to-terminal transition, `←` back, Escape close, selection preserve, timer disposal, narrow truncation; slice map in `docs/tests/issue-46-plan.md` — no other surfaces changed (`git diff 5c9fb9d...HEAD --name-only`: index.ts, navigator.mjs, navigator.ts, tests + docs) |
| Reachability/mutation classification is correct for changed surfaces | judgment | PASS | inventory class: `navigator.detail`=mutation (starts/stops a refresh interval, enters/leaves detail mode, closes overlay from detail; does not mutate run state — close/dismiss is #47); local extension seam, no HTTP/API routes |

### Depth

| Check | Method | Decision | Evidence |
|---|---|---|---|
| No test skipped/focused/xfail added | gate-lint | PASS | `lint-tests.mjs --rules no-skip --diff 5c9fb9d` → 0 findings |
| No fixed sleep added (waitForTimeout / numeric cy.wait / time.sleep) | gate-lint | PASS | `lint-tests.mjs --rules no-fixed-timeout --diff 5c9fb9d` → 0 findings (detail refresh uses injected fake timers; no real sleeps) |
| No empty/placeholder test body added (passes vacuously, asserts nothing) | gate-lint | PASS | `lint-tests.mjs --rules no-empty-test --diff 5c9fb9d` → 0 findings |
| Each added test actually asserts a behavior (not a no-op or helper-only render-health mislabeled as a journey) | lead | PASS | `tests/navigator_detail.test.mjs` — 18 tests, all `assert.equal`/`assert.ok`/`assert.deepEqual` on observable outcomes (detail field assembly from real registry+log, rendered field presence, Enter→detail, 1s timer cadence via fake intervals, running→terminal on tick without close, `←` back + selection restore, Escape close, dispose idempotency, width truncation, Pi custom()→done(null) dispose tracking + session_shutdown timer clear, custom() rejection clears dispose slot with zero unhandledRejection when return discarded); command: `node --test tests/navigator_detail.test.mjs` → 18/18 pass; full suite `node --test tests/*.test.mjs` |
| Declared @level meets each surface floor (no Wrong Level) | gate-lint | PASS | `@level unit` tags in `tests/navigator_detail.test.mjs` match `requiredLevels: [unit]` in `docs/tests/issue-46-detail-subagent-navigator.coverage.yml`; coverage-ledger → 16 aligned, 0 Wrong-Level (residual exit-1 rows are `Unverified` attestation only — see breadth row) |
| First-party interception in a behaviour/journey test is justified, not counted as real coverage | lead | N/A | no browser / Playwright tests in diff (`git diff 5c9fb9d...HEAD --name-only`) |
| No mock of a first-party internal seam module (real internals, faked externals) — zero-tolerance: PR-lane (new) AND whole-tree (inherited); no `@mock-ok` waiver; resolve by making it real, faking the external, or DELETING the test (over-mock is worse than no test); a false positive is fixed by correcting topology in coverage.config.json | gate-lint | PASS | `lint-tests.mjs --rules mock-internal-seam --diff 5c9fb9d` → 0 findings (PR lane); whole-tree → 0 findings; `scan-diff.mjs` findings channel `[]` — no `vi.mock`/`jest.mock` anywhere; tests use REAL `navigator.mjs` seams and REAL registry/parse for builder tests; only EXTERNAL boundaries are faked (pi UI/custom/theme, injected `setInterval`/`clearInterval`, matchKey/truncate) — per #43's testing decisions (pure state seam + thin wiring seam with fake Pi UI/editor/timer objects) |
| Mock use respects the boundary; money/auth/idempotency never mock-only | judgment | N/A | no mocks of internal seams; not a money/auth surface |
| Assertions are behavior-first; none pass on a 404 / empty / error page | judgment | PASS | assertions target externally visible behavior: what Enter opens, which fields render, how tick refresh updates status/output, what `←`/Escape do to mode/timers/selection, width contracts — never private helper internals beyond stable seams; command: `node --test tests/navigator_detail.test.mjs` |
| Every new branch / error path has a driving test | lead | PASS | `scan-diff.mjs` new-branch leads adjudicated: `buildNavigatorDetail` unknown-meta null + running vs terminal output/tool preference → builder tests; `buildDetailLines` missing detail / empty output / multi-line body → field + truncation tests; overlay `enterDetail` empty-list no-op → "Enter on an empty list is a no-op"; detail-mode left/escape vs list-mode enter/escape/up/down → Enter/back/Escape/selection tests; timer start/stop/dispose/idempotent → refresh + dispose tests; `selectById` missing id clamp → selection-preserve test with `getRows` dropping a row; `loadDetail` throw → guarded try/catch (fallback to row snapshot exercised when getDetail absent via row-field fallback in enterDetail); `openTrackedNavigator` sync onComponent capture + `disposeTrackedNavigator` session_shutdown path under Pi custom()→done(null) → "custom() resolving to done(null) still tracks dispose for session_shutdown"; custom() rejection path → "custom() rejection clears dispose slot without unhandledRejection" + runtime-smoke rejection drain (not source-only) |
| Diffs that modify existing behavior are pinned by a characterization test (prior behavior) before the change — or an intended change is declared with its AC | lead | PASS | intended changes declared against #46 ACs: (1) list help line gains `enter open`; (2) Enter is no longer an ignored list key (opens detail); (3) Escape from detail closes (same as list). Fix round 1: dispose capture moved from custom()-promise `.then(component)` (broken under Pi) to sync `onComponent` via `openTrackedNavigator` — pinned by Pi-compatible custom() fakes + session_shutdown timer-clear regression. Fix round 2: settlement cleanup `.finally` → handled `.then(clear, clear)` so discarded rejected custom() cannot unhandledRejection. #45 empty-list help pin updated to tolerate `enter` in help; #45 "unrelated keys" no longer presses Enter. Widget path UNCHANGED — non-regression pinned by existing `tests/widget_flicker.test.mjs` |
| Runtime smoke: each changed non-browser surface (service/API/CLI/job) was actually run via the repo run command and its happy path responded (boots, no 500/crash on first call) — green tests alone do not prove the thing runs; browser surfaces use the presentation sweep instead | gate-lint | PASS | `docs/tests/issue-46-runtime-smoke.json` → 2 surface(s), 0 fail; rerun: `node docs/tests/issue-46-runtime-smoke.mjs` (real tmpdir registry + log parse → detail fields; Enter→detail 1s timer; running→terminal on tick; `←` back keeps selection + disposes; Escape closes + disposes; Pi custom()→done(null); custom() rejection clears dispose slot + zero unhandledRejection when return discarded; narrow width; dispose idempotent; TUI guard intact; `node --experimental-strip-types --check index.ts` parses; **behavioral** session_shutdown + rejection paths via `openTrackedNavigator`/`disposeTrackedNavigator`) |

### Presentation

| Check | Method | Decision | Evidence |
|---|---|---|---|
| render-health is in the floor for every changed browser route | gate-lint | N/A | no browser routes in this pi extension (`git diff 5c9fb9d...HEAD --name-only` — no route/page files) |
| Presentation sweep ran (runtime gate): no pageerror/overflow/overlap/shift; nothing blocked | gate-lint | N/A | no browser routes |
| Sweep specs do not intercept first-party traffic | lead | N/A | no browser routes |
| Perceivability edge cases the instruments cannot score are checked | judgment | N/A | no browser routes |

### Enforcement

| Check | Method | Decision | Evidence |
|---|---|---|---|
| CI wires all required gates and keeps them blocking | gate-lint | PASS | `.github/workflows/ci.yml` runs the type-strip check + `node --test tests/*.test.mjs` on PRs (the gates applicable to this extension repo — no browser surfaces, so no sweep/e2e gates exist to wire; precedent: `docs/coverage-checklist-issue-45.md` enforcement row) |
