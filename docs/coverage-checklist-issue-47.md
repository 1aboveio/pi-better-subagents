## Coverage Checklist

_Every row = a decision (PASS / FAIL / N/A) backed by a checkable evidence locator (`file:line`, a rerunnable `command`, or #issue). Gate rows are auto-filled by `coverage-checklist.mjs generate`; lead/judgment rows are completed by the reviewer. A PASS/FAIL row with no locator, or a gate row left TODO, fails `coverage-checklist.mjs validate`._

Issue: #47 (two-press close behavior in the subagent navigator). Scope class: `surface-bearing` (`scope-class.mjs classify --stdin` over `6e7607d...HEAD` → unmatched runtime paths include `index.ts`, `navigator.mjs`, `navigator.ts`). Stacked unit: base is `issue-46-detail-subagent-navigator` (`6e7607d`), not `main`.

### Breadth

| Check | Method | Decision | Evidence |
|---|---|---|---|
| Surface inventory + route manifest regenerated; no drift | gate-lint | PASS | `docs/tests/_generated/surface-inventory.json` regenerated with `navigator.close` (mutation); no browser route manifest exists in this pi extension (precedent: `docs/coverage-checklist-issue-46.md`) |
| Every changed surface is owned and proven at its floor (no Missing/Orphan) | gate-lint | PASS | command: `node coverage-ledger.mjs validate` → "17 surfaces · 17 aligned · 0/17 tested surfaces over-mocked" — zero Missing/Orphan/Unrealized for the 1 #47 surface (owned by `docs/tests/issue-47-close-subagent-navigator.coverage.yml`, proven by `tests/navigator_close.test.mjs`); exit 1 is solely `Unverified` attestation rows (16 pre-existing across #13/#39/#44/#45/#46 + 1 #47 row a producer may not self-attest — awaiting reviewer promotion per ensure-coverage) |
| No un-specced capability missed — the surfaces no AC mentions | judgment | PASS | issue #47 ACs cover arming, footer confirm hints (stop vs dismiss), first-x non-mutation, same-selection second-x window, expiry, selection/list↔detail/overlay/teardown disarm, reread-before-act, running stop+kill+dismiss, terminal dismiss-only, finish-during-arm terminal dismissal, navigator visibility hide + tool API access; slice map in `docs/tests/issue-47-plan.md` — no other surfaces changed (`git diff 6e7607d...HEAD --name-only`: index.ts, navigator.mjs, navigator.ts, tests + docs) |
| Reachability/mutation classification is correct for changed surfaces | judgment | PASS | inventory class: `navigator.close`=mutation (arms/disarms confirmation, calls shared stopRun + dismissRun, refreshes visible rows + footer hints); local extension seam, no HTTP/API routes |

### Depth

| Check | Method | Decision | Evidence |
|---|---|---|---|
| No test skipped/focused/xfail added | gate-lint | PASS | `lint-tests.mjs --rules no-skip --diff 6e7607d` → 0 findings |
| No fixed sleep added (waitForTimeout / numeric cy.wait / time.sleep) | gate-lint | PASS | `lint-tests.mjs --rules no-fixed-timeout --diff 6e7607d` → 0 findings (close arm uses injected fake clock/setTimeout; process-exit uses bounded waitFor poll, not fixed sleep) |
| No empty/placeholder test body added (passes vacuously, asserts nothing) | gate-lint | PASS | `lint-tests.mjs --rules no-empty-test --diff 6e7607d` → 0 findings |
| Each added test actually asserts a behavior (not a no-op or helper-only render-health mislabeled as a journey) | lead | PASS | `tests/navigator_close.test.mjs` — 19 tests, all `assert.equal`/`assert.ok`/`assert.deepEqual` on observable outcomes (hint text stop vs dismiss, first-x non-mutation, arm window exclusive at +3s, second-x same-id act, expiry re-arm without act, selection/list↔detail/escape/dispose disarm, running stop+kill+dismiss with real sleep process group, terminal dismiss preserves status, finish-during effective exited → dismiss only, tools list/output/result still resolve dismissed); plus `tests/navigator_close_extension_path.test.mjs` — 3 integration tests loading the real `index.ts` factory → `session_start` → empty-editor ← open → first+second `x` for terminal dismiss and running stop+kill+dismiss via shared `stopRun` (closes integrated-review P0 `navigator.close.integration-wiring`); command: `node --test tests/navigator_close.test.mjs tests/navigator_close_extension_path.test.mjs` → 22/22 pass; full suite `node --test tests/*.test.mjs` → green |
| Declared @level meets each surface floor (no Wrong Level) | gate-lint | PASS | `@level unit` tags in `tests/navigator_close.test.mjs` match `requiredLevels: [unit]` in `docs/tests/issue-47-close-subagent-navigator.coverage.yml`; coverage-ledger → 17 aligned, 0 Wrong-Level (residual exit-1 rows are `Unverified` attestation only — see breadth row) |
| First-party interception in a behaviour/journey test is justified, not counted as real coverage | lead | N/A | no browser / Playwright tests in diff (`git diff 6e7607d...HEAD --name-only`) |
| No mock of a first-party internal seam module (real internals, faked externals) — zero-tolerance: PR-lane (new) AND whole-tree (inherited); no `@mock-ok` waiver; resolve by making it real, faking the external, or DELETING the test (over-mock is worse than no test); a false positive is fixed by correcting topology in coverage.config.json | gate-lint | PASS | `lint-tests.mjs --rules mock-internal-seam --diff 6e7607d` → 0 findings (PR lane); whole-tree → 0 findings; `scan-diff.mjs` findings channel `[]` — no `vi.mock`/`jest.mock` anywhere; tests use REAL `navigator.mjs` seams, REAL `stopRun`/`dismissRun`/registry, and a real detached `sleep 30` for running-close; only EXTERNAL boundaries are faked (pi UI/custom/theme, injected clock/setTimeout/setInterval, matchKey/truncate) — per #43's testing decisions (pure state seam + thin wiring seam with fake Pi UI/editor/timer objects) |
| Mock use respects the boundary; money/auth/idempotency never mock-only | judgment | N/A | no mocks of internal seams; not a money/auth surface |
| Assertions are behavior-first; none pass on a 404 / empty / error page | judgment | PASS | assertions target externally visible behavior: confirm hint text, first-x non-mutation of meta/process, second-x stop/dismiss outcomes, arm expiry, selection/mode/teardown disarm, visibility hide + tool recovery — never private helper internals beyond stable seams; command: `node --test tests/navigator_close.test.mjs` |
| Every new branch / error path has a driving test | lead | PASS | `scan-diff.mjs` new-branch leads adjudicated: `closeConfirmHint` name fallback / running vs terminal → hint tests; `isCloseArmed` id/window boundary → arm window test; `executeNavigatorClose` missing/running/terminal/finish-during → execute tests; overlay `handleCloseKey` first vs second press, selection disarm, enter/leave detail disarm, escape/dispose disarm, detail-mode close + list refresh → overlay two-press suite; `clearCloseArm` / arm timer expiry → expiry + dispose tests; unknown id missing outcome → execute missing test; tool compatibility after dismiss → tools suite. Test-file helper branches (drive harness) are test infrastructure, not product branches |
| Diffs that modify existing behavior are pinned by a characterization test (prior behavior) before the change — or an intended change is declared with its AC | lead | PASS | intended changes declared against #47 ACs: (1) list/detail help lines gain `x close`; (2) `x` is no longer an ignored key (arms/acts Close); (3) session_shutdown also clears `CLOSE_CONFIRM_STATUS_KEY`. #45 "unrelated keys" test updated to press `z`/`q` instead of `x` (declared intended change — Close ownership moves to #47 suite). Widget path UNCHANGED — non-regression pinned by existing `tests/widget_flicker.test.mjs`. Non-TUI isolation unchanged (`isNavigatorUiAvailable`) |
| Runtime smoke: each changed non-browser surface (service/API/CLI/job) was actually run via the repo run command and its happy path responded (boots, no 500/crash on first call) — green tests alone do not prove the thing runs; browser surfaces use the presentation sweep instead | gate-lint | PASS | `docs/tests/issue-47-runtime-smoke.json` → 1 surface(s), 0 fail; rerun: `node docs/tests/issue-47-runtime-smoke.mjs` (real tmpdir registry + shared stopRun/dismissRun; running→killed+dismissed with process group gone; terminal dismiss preserves status; finish-during→exited dismiss no kill rewrite; overlay two-press within 3s; expiry/dispose disarm non-mutating; footer confirm coexists with count; tools list/output/result still resolve dismissed; TUI guard; `index.ts` imports `stopRun` from `./stop.ts` for `navigatorCloseRun`; strip-types check; registered extension-path second-x suite green) |

### Presentation

| Check | Method | Decision | Evidence |
|---|---|---|---|
| render-health is in the floor for every changed browser route | gate-lint | N/A | no browser routes in this pi extension (`git diff 6e7607d...HEAD --name-only` — no route/page files) |
| Presentation sweep ran (runtime gate): no pageerror/overflow/overlap/shift; nothing blocked | gate-lint | N/A | no browser routes |
| Sweep specs do not intercept first-party traffic | lead | N/A | no browser routes |
| Perceivability edge cases the instruments cannot score are checked | judgment | N/A | no browser routes |

### Enforcement

| Check | Method | Decision | Evidence |
|---|---|---|---|
| CI wires all required gates and keeps them blocking | gate-lint | PASS | `.github/workflows/ci.yml` runs the type-strip check + `node --test tests/*.test.mjs` on PRs (the gates applicable to this extension repo — no browser surfaces, so no sweep/e2e gates exist to wire; precedent: `docs/coverage-checklist-issue-46.md` enforcement row) |
