## Coverage Checklist

_Every row = a decision (PASS / FAIL / N/A) backed by a checkable evidence locator (`file:line`, a rerunnable `command`, or #issue). Gate rows are auto-filled by `coverage-checklist.mjs generate`; lead/judgment rows are completed by the reviewer. A PASS/FAIL row with no locator, or a gate row left TODO, fails `coverage-checklist.mjs validate`._

Issue: #45 (minimal subagent navigator from empty-editor ←). Scope class: `surface-bearing` (`scope-class.mjs classify --stdin` over `cf2cb24...HEAD` → "1 path(s) match no configured non-runtime pattern — index.ts"). Stacked unit: base is `issue-44-navigator-visibility-stop` (`cf2cb24`), not `main`.

### Breadth

| Check | Method | Decision | Evidence |
|---|---|---|---|
| Surface inventory + route manifest regenerated; no drift | gate-lint | PASS | `docs/tests/_generated/surface-inventory.json` regenerated with `navigator.footer-hint` (mutation), `navigator.editor-wrapper` (mutation), `navigator.overlay` (read); no browser route manifest exists in this pi extension (precedent: `docs/coverage-checklist-issue-44.md`) |
| Every changed surface is owned and proven at its floor (no Missing/Orphan) | gate-lint | PASS | command: `node coverage-ledger.mjs validate` → "15 surfaces · 15 aligned · 0/15 tested surfaces over-mocked" — zero Missing/Orphan/Unrealized for the 3 #45 surfaces (owned by `docs/tests/issue-45-minimal-subagent-navigator.coverage.yml`, proven by `tests/navigator_overlay.test.mjs`); exit 1 is solely `Unverified` attestation rows (8 pre-existing: 5 #13 `widget.*` + 3 #39 `sandbox.*`; 4 #44 rows awaiting #44's reviewer; 3 #45 rows a producer may not self-attest — awaiting reviewer promotion per ensure-coverage) |
| No un-specced capability missed — the surfaces no AC mentions | judgment | PASS | issue #45 ACs cover the footer hint, empty-editor interception/delegation, wrapper composition + no-stacking, the overlay list/selection/Escape, widget non-regression, and non-TUI inactivity; slice map in `docs/tests/issue-45-plan.md` — no other surfaces changed (`git diff cf2cb24...HEAD --name-only`: index.ts, navigator.mjs, navigator.ts, tests + docs) |
| Reachability/mutation classification is correct for changed surfaces | judgment | PASS | inventory classes: `navigator.footer-hint`=mutation (publishes/clears a footer status), `navigator.editor-wrapper`=mutation (intercepts input, installs the editor factory, opens the overlay), `navigator.overlay`=read (renders the list + moves selection; no run-state mutation in #45 — close/dismiss is #47); local extension seams, no HTTP/API routes |

### Depth

| Check | Method | Decision | Evidence |
|---|---|---|---|
| No test skipped/focused/xfail added | gate-lint | PASS | `lint-tests.mjs --rules no-skip --diff cf2cb24` → 0 findings |
| No fixed sleep added (waitForTimeout / numeric cy.wait / time.sleep) | gate-lint | PASS | `lint-tests.mjs --rules no-fixed-timeout --diff cf2cb24` → 0 findings |
| No empty/placeholder test body added (passes vacuously, asserts nothing) | gate-lint | PASS | `lint-tests.mjs --rules no-empty-test --diff cf2cb24` → 0 findings |
| Each added test actually asserts a behavior (not a no-op or helper-only render-health mislabeled as a journey) | lead | PASS | `tests/navigator_overlay.test.mjs` — 26 tests, all `assert.equal`/`assert.deepEqual`/`assert.ok` on observable outcomes (status key + hint text, guard verdicts per real ctx shape incl. RPC, ui-call isolation behind the guard, open-callback firing vs delegation input lists, factory install counts, row id ordering/content, selection index + `> ` row prefix, line widths, done-calls on Escape, real `pi --mode rpc` startup-through-shutdown emits zero navigator `setStatus`); command: `node --test tests/navigator_overlay.test.mjs` → 26/26 pass; full suite `node --test tests/*.test.mjs` → 137 pass / 0 fail / 0 skipped |
| Declared @level meets each surface floor (no Wrong Level) | gate-lint | PASS | `@level unit` tags in `tests/navigator_overlay.test.mjs` match `requiredLevels: [unit]` in `docs/tests/issue-45-minimal-subagent-navigator.coverage.yml`; `coverage-ledger.mjs validate` → 15 aligned, 0 Wrong-Level (residual exit-1 rows are `Unverified` attestation only — see breadth row) |
| First-party interception in a behaviour/journey test is justified, not counted as real coverage | lead | N/A | no browser / Playwright tests in diff (`git diff cf2cb24...HEAD --name-only`) |
| No mock of a first-party internal seam module (real internals, faked externals) — zero-tolerance: PR-lane (new) AND whole-tree (inherited); no `@mock-ok` waiver; resolve by making it real, faking the external, or DELETING the test (over-mock is worse than no test); a false positive is fixed by correcting topology in coverage.config.json | gate-lint | PASS | `lint-tests.mjs --rules mock-internal-seam --diff cf2cb24` → 0 findings (PR lane); `lint-tests.mjs --rules mock-internal-seam` → 0 findings (whole-tree); `scan-diff.mjs` findings channel `[]` — no `vi.mock`/`jest.mock` anywhere; tests use the REAL registry (`registry.ts`, tmpdir-backed) and REAL `navigator.mjs` seams; only EXTERNAL boundaries are faked (pi's `ui.setStatus`/`ui.custom`/`getEditorComponent`, the pi Editor, pi-tui `matchesKey`/`truncateToWidth`, theme) — per #43's testing decisions (pure state seam + thin wiring seam with fake Pi UI/editor objects) |
| Mock use respects the boundary; money/auth/idempotency never mock-only | judgment | N/A | no mocks of internal seams; not a money/auth surface |
| Assertions are behavior-first; none pass on a 404 / empty / error page | judgment | PASS | assertions target externally visible behavior: what input opens/doesn't open the navigator, what footer status is published, what rows/ordering render, how selection moves, what Escape resolves — never implementation internals; command: `node --test tests/navigator_overlay.test.mjs` |
| Every new branch / error path has a driving test | lead | PASS | `scan-diff.mjs` new-branch leads adjudicated: `navigator.mjs:21` hint ternary → "hint text is `← subagents · N` for N ≥ 1 and null at zero"; `:75` `rows ?? []` → trivial default (callers always pass arrays; clamp covers empty) + "an empty list renders an explicit placeholder"; `:82-84` clamp → "clamps a selection that outlives its row list"; `:117` empty-rows line → "an empty list renders an explicit placeholder (defensive; open normally refuses empty)"; `:122`/`:124` prefix/spend → overlay list + row-field tests; `:149` theme fallback → presentational guard (theme always provided by pi; unthemed fallback exercised implicitly when `theme.fg` absent — adjudicated trivial); `:154-156` styling branches → "opens as a focused overlay …" shape assertions; `:161-165` key branches → Up/Down/Escape/unrelated-key tests; `:214-224` proxy get/set/bind → interception + composition tests (method bind via `getText`/`setText` through the wrapper; plain property via `onSubmit`); `:251-252` install branches → three install tests (no prior factory / compose with previous / refresh-no-stack); `index.ts:223-244` UI guards + dirty-check → `isNavigatorUiAvailable` unit tests incl. the real RPC-shaped context (`mode:rpc` + `hasUI:true` + `ui`) returning false and reaching no setStatus/setEditorComponent/custom wiring, print/JSON/no-mode fail-closed pins, + source-scan pins; `index.ts` `session_shutdown` navigator cleanup branch → "every navigator entry point in index.ts is behind the TUI-mode guard" (source pin: shutdown body requires `isNavigatorUiAvailable(ctx)` before `setStatus(NAVIGATOR_STATUS_KEY,…)`) + live "real pi RPC startup-through-shutdown emits no navigator setStatus" (drives the registered handler via real `pi --mode rpc`, would fail at bc0d0f); `navigator.mjs` `isNavigatorUiAvailable` mode/hasUI/ui branches → "navigator UI is available only in TUI mode with a UI" (8 shapes: tui+ui pass, rpc blocked, print/json blocked, no-mode fail-closed, tui without ui blocked, undefined blocked) |
| Diffs that modify existing behavior are pinned by a characterization test (prior behavior) before the change — or an intended change is declared with its AC | lead | PASS | two intended behavior changes across fix rounds, both against the #45 AC "inactive in non-TUI modes": (1) Round 1 — `isNavigatorUiAvailable` requires `ctx.mode === "tui"` (pi docs: hasUI is true in TUI AND RPC); (2) Round 2 — `session_shutdown` navigator `setStatus` cleanup now sits behind the same guard (at bc0d0f the live RPC probe still emitted `extension_ui_request` `setStatus`/`subagents-nav` on shutdown). TDD: Round 2 live RPC probe + source-scan pins fail at bc0d0f, pass after the guard. Widget path intentionally still clears via `setWidget` in RPC (pi docs: setWidget works in both modes) — non-regression pinned by UNCHANGED `tests/widget_flicker.test.mjs` (25 tests); full suite 137/137 |
| Runtime smoke: each changed non-browser surface (service/API/CLI/job) was actually run via the repo run command and its happy path responded (boots, no 500/crash on first call) — green tests alone do not prove the thing runs; browser surfaces use the presentation sweep instead | gate-lint | PASS | `docs/tests/issue-45-runtime-smoke.json` → 4 surface(s), 0 fail; rerun: `node docs/tests/issue-45-runtime-smoke.mjs` (real tmpdir registry drives the footer count + hint publish/clear; real key sequences through the installed editor factory — open vs delegate, 2 installs → 1 factory; real registry rows newest-first rendered through `showNavigator` with Down-move + Escape-close; guard proven at the seam boundary: rpc blocked / print blocked / tui allowed; `node --experimental-strip-types --check index.ts` parses; **real `pi --mode rpc` startup-through-shutdown emits 0 navigator setStatus** — the live proof that would fail at bc0d0f) |

### Presentation

| Check | Method | Decision | Evidence |
|---|---|---|---|
| render-health is in the floor for every changed browser route | gate-lint | N/A | no browser routes in this pi extension (`git diff cf2cb24...HEAD --name-only` — no route/page files) |
| Presentation sweep ran (runtime gate): no pageerror/overflow/overlap/shift; nothing blocked | gate-lint | N/A | no browser routes |
| Sweep specs do not intercept first-party traffic | lead | N/A | no browser routes |
| Perceivability edge cases the instruments cannot score are checked | judgment | N/A | no browser routes |

### Enforcement

| Check | Method | Decision | Evidence |
|---|---|---|---|
| CI wires all required gates and keeps them blocking | gate-lint | PASS | `.github/workflows/ci.yml` runs the type-strip check + `node --test tests/*.test.mjs` on PRs (the gates applicable to this extension repo — no browser surfaces, so no sweep/e2e gates exist to wire; precedent: `docs/coverage-checklist-issue-44.md` enforcement row) |
